# Connect express with socketio example
Example how to connect express with socketio

## Installation using yarn
1. install dependency `yarn`

## Installation using npm
1. install dependency `npm i`

## Run
1. yarn dev
